import React from 'react';
import { useParams } from 'react-router-dom';

interface RouteParams {
  countrySlug: string,
  country: string,
  region: string
}

export default () => {
  return (
      <div>
      </div>
  )
}

